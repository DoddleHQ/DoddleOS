id: doddle.marketing.public-relations
version: 1.5.1
kind: skill
engine: doddle-os>=1.0
description: "PR strategy, press releases and media outreach"
inputs:
  - {name: context, type: string, required: true, description: "Story, announcement, audience, and media targets"}
  - {name: goal, type: string, required: false, description: "PR goal, ex coverage, launches, crisis response"}
outputs:
  - {name: report, type: markdown, description: "PR angle, release draft, pitch list and outreach plan"}
  - {name: recommendations, type: json, description: "Prioritized media targets with pitch angles"}
tools:
  - {id: doddle.tool.v1.ga4.getReport, required: false, fallback: manual_review}
  - {id: doddle.tool.v1.hubspot.contacts, required: false, fallback: manual_review}
  - {id: doddle.tool.v1.notion.pages, required: false, fallback: manual_review}
integrations:
  - google-analytics
  - hubspot
  - notion
nodes:
  - {id: assess, type: agent, ref: researcher, needs: [inputs], produces: [assessment]}
  - {id: analyze, type: tool, ref: doddle.tool.v1.ga4.getReport, needs: [assess], produces: [analytics_data]}
  - {id: recommend, type: agent, ref: copywriter, needs: [analyze], produces: [report, recommendations]}
  - {id: plan, type: agent, ref: planner, needs: [recommend], produces: [action_plan]}
on_fail: {retry: 1, fallback: manual_review}
