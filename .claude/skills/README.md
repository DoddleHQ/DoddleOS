# Moved: Doddle Marketing OS pack

Marketing skills moved to `doddle-marketing-skills/.claude/skills/` as an independent peer pack (49 skills, `doddle.marketing.*`).

- Registry: `doddle-core/registry/packs.json` (14 peer packs)
- Validator discovers `doddle-*/.claude/skills/*/` — no hardcoded paths
- This dir intentionally empty (peer model, DoddleOS v2)
